# Frontend (Vercel)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production:
# NEXT_PUBLIC_API_URL=https://your-api.onrender.com

# Vercel (frontend + backend same project): leave NEXT_PUBLIC_API_URL UNSET
# The browser will call /api/... on the same domain automatically.
